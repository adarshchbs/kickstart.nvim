-- This file ensures that all custom type definitions are loaded
-- It's automatically loaded by Neovim when the 'lua/custom/types' directory is in the runtimepath
-- Load all type definition files
require('custom.types.conform')
require('custom.types.telescope')
